[build-system]
requires = [
    "setuptools >= 54.0.2",
    "setuptools_scm >= 6, <7",
]
build-backend = "setuptools.build_meta"

[tool.black]
target_version = ['py36', 'py37', 'py38']
skip-string-normalization = true
